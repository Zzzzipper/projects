import { a as stringSize, b as createPadding } from '../common/_createPadding-23ab668f.js';
import { t as toInteger } from '../common/toInteger-4c60f77a.js';
import { t as toString } from '../common/toString-8f2e88a2.js';
import '../common/_baseSlice-9996a8b8.js';
import '../common/_baseProperty-8760c68e.js';
import '../common/toNumber-6a88c66b.js';
import './isObject.js';
import '../common/isSymbol-04af84fe.js';
import '../common/isObjectLike-13d75f34.js';
import '../common/isArray-3d6a7c93.js';

/**
 * Pads `string` on the right side if it's shorter than `length`. Padding
 * characters are truncated if they exceed `length`.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category String
 * @param {string} [string=''] The string to pad.
 * @param {number} [length=0] The padding length.
 * @param {string} [chars=' '] The string used as padding.
 * @returns {string} Returns the padded string.
 * @example
 *
 * _.padEnd('abc', 6);
 * // => 'abc   '
 *
 * _.padEnd('abc', 6, '_-');
 * // => 'abc_-_'
 *
 * _.padEnd('abc', 3);
 * // => 'abc'
 */
function padEnd(string, length, chars) {
  string = toString(string);
  length = toInteger(length);

  var strLength = length ? stringSize(string) : 0;
  return (length && strLength < length)
    ? (string + createPadding(length - strLength, chars))
    : string;
}

export default padEnd;
//# sourceMappingURL=padEnd.js.map
