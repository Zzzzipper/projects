import { RetrospectiveState } from './types';

const INITIAL_STATE: RetrospectiveState = {
    retrospective: undefined,
    module: {
        mod_id: null,
        obj_with_mod_id: null,
    }
};

export default INITIAL_STATE;
